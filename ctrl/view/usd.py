import os
import ctrl.config as config

def run_usd(path_to_usd):
    usdview_path = os.path.join(config.USD_ROOT_PATH, 'bin', 'usdview')

def run_usdrecord(path_to_usd, output_path):
    usdrecord_path = os.path.join(config.USD_ROOT_PATH, 'bin', 'usdview')
